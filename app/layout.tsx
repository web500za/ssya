import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SSYA — Southern Suburbs Youth Academy | Finish Your Matric",
  description:
    "Complete your matric with SSYA. Affordable adult education in Cape Town's Southern Suburbs. Qualified educators, flexible classes. Ages 18–65.",
  openGraph: {
    title: "SSYA — Southern Suburbs Youth Academy",
    description:
      "Complete your matric with SSYA. Affordable adult education in Cape Town's Southern Suburbs.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
